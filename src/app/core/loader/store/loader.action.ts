// src/app/store/actions/loader.actions.ts
import { createAction } from '@ngrx/store';

export const showLoader = createAction('[Loader] Show');
export const hideLoader = createAction('[Loader] Hide');
